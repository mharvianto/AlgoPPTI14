#include<stdio.h>
#include<string.h>

int main() {
    // strcmp -> compare string
    char nama[100];
    scanf("%s", nama);
    // if (strcmp(nama, "budi") == 0){
    //     printf("Selamat pagi\n");
    // }
    // int flag = 0;
    // char budi[] = "budi";
    // int l = strlen(budi);
    // for (int i = 0; i < l; i++) {
    //     if(nama[i] < budi[i]) {
    //         flag = -1;
    //         break;
    //     } else if(nama[i] > budi[i]) {
    //         flag = 1;
    //         break;
    //     }
    // }
    // strcpy -> string copy

    // char temp[100];
    // strcpy(temp, nama);
    // printf("%s\n", temp);
    // int l2 = strlen(nama);
    // for (int i = 0; i <= l2; i++) {
    //     temp[i] = nama[i]; 
    // }
    // printf("%s\n", temp);
    
    return 0;
}